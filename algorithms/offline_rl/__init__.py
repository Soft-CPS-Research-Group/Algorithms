"""Offline RL package.

This package mirrors the v1 ``algorithms/offline/`` tree but lives in parallel.
Owns schema, data loader, reward and trainers for the offline pipeline.

See ``OFFLINE_RL_AGENTS.md`` at the repo root for context.
"""
